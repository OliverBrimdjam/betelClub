import React from 'react';
import { CartContainer } from './styles';

const Cart: React.FC = () => {
    return (
        <CartContainer>
            <h1>Vai!</h1>
        </CartContainer>

    );
};

export default Cart;
