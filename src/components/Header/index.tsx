import React from 'react';
import { HeaderContainer } from './styles';

const Header: React.FC = () => {
    return (
        <HeaderContainer>
            <h1>vai chegar</h1>
        </HeaderContainer>
    );
};

export default Header;