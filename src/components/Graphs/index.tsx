import React from 'react';

import { GraphsContainer } from './styles';

const Graphs: React.FC = () => {
    return (

        <GraphsContainer>
            <h1>Vai!</h1>
        </GraphsContainer>
    );
};

export default Graphs;